import type { Request, Response } from 'express'
import * as groupService from '../service/group.service'
import { apiError } from '../utils/ApiError'
import { apiResponse } from '../utils/ApiResponse'

export const registerGenre = (req: Request, res: Response) => {

}

export const createGroup = (req: Request, res: Response) => {

}

export const abort = (req: Request, res: Response) => {
    Yha abort krne se phle must check ki jo abort krra h wo uss group ka player h bhi ya ni
}